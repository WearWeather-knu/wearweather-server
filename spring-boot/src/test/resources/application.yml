spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:wearweather-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
